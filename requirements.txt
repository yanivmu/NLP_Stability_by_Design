# Core dependencies for Stability by Design experiments
torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# For Llama model access (requires HuggingFace authentication)
huggingface_hub>=0.16.0

# Perturbation generation (TM3 - WordNet synonym replacement)
nltk>=3.8.0
